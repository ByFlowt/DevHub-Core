import React from 'react';
import Sidebar from "../components/Sidebar/sidebar.tsx";

const Pipelines = () => {
    return (
        <div>
            <Sidebar />
        </div>
    );
};

export default Pipelines;